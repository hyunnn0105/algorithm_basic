package Baekjoon.function;

public class Bj4673_selfNum {
/*
    // 함수
    public static int selfNum(int number){
        int sum = number;

        while (number != 0){
            sum += number%10;
            number = number/10;
        }

        return sum;
    }

    public static void main(String[] args) {
        int index = 10000;
        boolean[] number = new boolean[index];
        for (int i = 1; i < number.length; i++) {
            allNumber.add(i);
        }
        System.out.println(allNumber.size());

        for (int i = 1; i < 101; i++) {
            // self-num 구하는 수
            dn = i + (i/10) + (i%10);

            // 생성자 없는 수 가려내기
            if (allNumber.contains(dn)){
                allNumber.remove(i);
            }

        }
        for (int j = 0; j < allNumber.size(); j++) {
            System.out.println(allNumber.get(j));;
        }
    }
 */
}


